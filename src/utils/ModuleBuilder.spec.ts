import * as angular from "angular";
import "angular-mocks";
import {ModuleBuilder} from "./ModuleBuilder";

describe("ModuleBuilder", () => {

    it("should do something", () => {

        expect(!!ModuleBuilder).toEqual(true);

    });
});